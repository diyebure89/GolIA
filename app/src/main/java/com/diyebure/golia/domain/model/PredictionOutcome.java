package com.diyebure.golia.domain.model;

/**
 * Enum representing the possible outcomes of a football match prediction.
 */
public enum PredictionOutcome {
    /** Home team wins */
    HOME_WIN,
    /** Match ends in a draw */
    DRAW,
    /** Away team wins */
    AWAY_WIN
}